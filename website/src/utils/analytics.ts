// Both Cal.com booking flows (onboarding and demo) redirect to /demo/success, so
// the flow a visitor was last in is stored under this key to attribute the booking.
export const BOOKING_FLOW_STORAGE_KEY = 'distr_booking_flow';
